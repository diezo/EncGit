## EncGit - An Encrypted VCS Written in Java
An encrypted version control sytem written in Java

## Commands
Here is the list of CLI commands supported till now:

- *init* - Initializes the repository
- *add* - Adds files to staging area
- *commit* - Saves a snapshot of stabing area as a commit
